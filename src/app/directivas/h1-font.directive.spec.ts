import { H1FontDirective } from './h1-font.directive';

describe('H1FontDirective', () => {
  it('should create an instance', () => {
    const directive = new H1FontDirective();
    expect(directive).toBeTruthy();
  });
});
