import { BooleanSiNoPipe } from './boolean-si-no.pipe';

describe('BooleanSiNoPipe', () => {
  it('create an instance', () => {
    const pipe = new BooleanSiNoPipe();
    expect(pipe).toBeTruthy();
  });
});
